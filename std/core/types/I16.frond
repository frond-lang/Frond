// I16.frond — the i16 integer type module (part of std.core.types)
//
// Boundary constants + decimal parse. parse accepts an optional leading
// '+'/'-' and returns Throw<_, ParseError>: Malformed on bad syntax (empty,
// bad sign position, non-digit), Overflow on values outside the i16 range —
// the same overflow-to-error contract as the float family (F*.parse), never
// a silent wrap.

pub val MAX: i16 = 32767
pub val MIN: i16 = -32768

pub fun parse(s: str): Throw<i16, ParseError> {
    val b = s.bytes()
    if b.len() == 0 { throw ParseError(Malformed, "empty input is not a i16 literal", s) }
    var neg = false
    var i = 0 as usize
    if b[0] == 43 { i = 1 as usize }
    if b[0] == 45 { neg = true; i = 1 as usize }
    if i >= b.len() { throw ParseError(Malformed, "sign with no digits is not a i16 literal", s) }
    var v: u128 = 0
    val limit: u128 = if neg { 32768 } else { 32767 }
    while i < b.len() {
        val d = (b[i] as u128) - 48
        if d > 9 { throw ParseError(Malformed, "invalid digit in i16 literal", s) }
        if v > (limit - d) / 10 { throw ParseError(Overflow, "value out of range for i16", s) }
        v = v * 10 + d
        i = i + 1
    }
    if neg { Ok((0 - (v as i128)) as i16) } else { Ok(v as i16) }
}
